use egui_cosmic_text::widget::*;

fn main() {
    // Test to see what enum values are available
    let _interactivity = Interactivity::; // This will show available values in error
    let _hover = HoverStrategy::; // This will show available values in error
    let _line_height = LineHeight::; // This will show available values in error
}